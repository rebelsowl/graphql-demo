type Player {
    id: ID!
    name: String
    surname: String
    position: String
}

input PlayerDto {
    name: String!
    surname: String!
    position: String!
}

type Query {
    getPlayers: [Player]
}

type Mutation {
    addPlayer(player: PlayerDto): Player
    deletePlayer(id: ID): String
}